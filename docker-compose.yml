services:
  kuma-lite:
    image: kuma-lite:latest
    container_name: kuma-lite
    ports:
      - "${SERVER_PORT:-8080}:8080"
    volumes:
      - ./data:/data
    env_file:
      - .env
    restart: unless-stopped
